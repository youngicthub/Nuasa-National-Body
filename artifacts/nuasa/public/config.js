/**
 * NUASA runtime configuration
 * Loaded before the React bundle — sets the live Replit API URL.
 *
 * AFEESHOST (production): URL below points to the live Replit backend.
 * DEVELOPMENT (Replit):   Leave the line commented out so the Vite proxy handles /api.
 */
// window.__NUASA_API_URL__ = "https://nuasa-national-body--nuasahost.replit.app/api";
