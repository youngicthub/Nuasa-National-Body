ALTER TABLE public.convention_registrations DROP CONSTRAINT IF EXISTS convention_registrations_registration_type_check; ALTER TABLE public.convention_registrations ADD CONSTRAINT convention_registrations_registration_type_check CHECK (registration_type IN ('student','graduate','chapter')); UPDATE public.convention_registrations SET registration_type = 'graduate', amount = 20000, updated_at = now() WHERE registration_type = 'delegate';